/**
 * Centralised theme definitions for ADN Studio.
 *
 * Use this module to keep colour values, spacing and other design constants
 * consistent across your app.  You can import the values defined here
 * directly into your components or pages.
 */

const theme = {
  colors: {
    background: '#0B0F2A', // Dark background used throughout the app
    gold: '#FFD700',       // Primary accent colour (gold)
    accent: '#7B61FF',     // Secondary accent colour (purple)
    text: '#FFFFFF'        // Default text colour on dark backgrounds
  },
  borderRadius: '1rem'
};

export default theme;